var pokemon=prompt("Pokemon whose details are needed to be displayed:");
displayInfo(pokemon);


var nextEvo=prompt("Next Evolution of which pokemon:");
nextEvolution(nextEvo);


var weak=prompt("Weakness:");
showWeaknesses(weak)
